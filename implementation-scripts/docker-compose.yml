version: '3.8'

services:
  grobid:
    image: grobid/grobid:latest
    restart: unless-stopped
    ports:
      - "9080:8070"  # Ports Host:Container
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    ulimits:
      core: 0